/**
 * Shared design tokens for the card: page size, panel radius, the one box style and the text inks.
 * Kept in one place so the card and its icon tiles never drift apart in fill, border or radius.
 */

export const PAGE = { w: 1200, h: 630 };

export const RADIUS = 22;

// One shared box style: the card and the icon tiles use the exact same fill, border and radius.
export const BOX_BG = 'rgba(9,11,15,0.78)';
export const BOX_BORDER = '#ffffff';
export const BOX_BORDER_WIDTH = 3;

export const INK = '#ededed';
export const DIM = '#9a9a9a';
